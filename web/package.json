{
  "name": "track_me",
  "version": "1.0.0",
  "description": "A web interface ti view TrackMe data",
  "main": "server.js",
  "scripts": {
    "start": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "git subtree push --prefix web heroku master"
  },
  "author": "Brenton Fleming",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.3"
  },
  "devDependencies": {
    "heroku": "^7.7.7",
    "nodemon": "^1.18.3"
  }
}
